# Epic 2 — Scope Fidelity Check (Task F4)

## Task-by-Task Compliance [11/14]

### Task 1: Schema Migration
**Spec**:
- Create `migrations/0002_expand_snapshots.sql` with 7 `ALTER TABLE` statements
- Remove inline `CREATE TABLE IF NOT EXISTS snapshots` from `src/db/snapshots.py`
- Keep index creation

**Actual**:
- `migrations/0002_expand_snapshots.sql` exists with 7 `ALTER TABLE` statements ✅
- Inline `CREATE TABLE IF NOT EXISTS snapshots` removed from `src/db/snapshots.py` ✅
- `CREATE INDEX IF NOT EXISTS snapshots_ticker_created_at_idx` preserved ✅

**Verdict**: ✅ COMPLIANT

### Task 2: DB Query Functions
**Spec**:
- Add 5 functions to `src/db/snapshots.py`
- Export them in `src/db/__init__.py`
- Extend `_upsert_snapshot_metadata()` with new columns

**Actual**:
- Added: `get_snapshot_metadata`, `list_snapshots`, `mark_snapshots_archived`, `delete_snapshots`, `get_snapshot_by_path` ✅
- Exported in `src/db/__init__.py` ✅
- `_upsert_snapshot_metadata()` now inserts `snapshot_path`, `rows`, `checksum`, `job_id`, `size_bytes` ✅

**Verdict**: ✅ COMPLIANT

### Task 3: `pipeline snapshot` command
**Spec**:
- Add command in `src/pipeline.py`
- Read from DB, generate CSV, handle empty/invalid ticker with exit 1
- Create output dir, use `CliFeedback`

**Actual**:
- Command implemented in `src/pipeline.py` (`@app.command("snapshot")`) ✅
- Reads via `db.read_prices`, handles empty with `CliFeedback.error` + `typer.Exit(1)` ✅
- Creates output dir and writes snapshot ✅
- Uses `CliFeedback` ✅

**Verdict**: ✅ COMPLIANT

### Task 4: Checksum metadata integration
**Spec**:
- Register full metadata after snapshot generation, including payload with extra metadata
- Ensure idempotency

**Actual**:
- Registers `ticker`, `created_at`, `snapshot_path`, `rows`, `checksum`, `size_bytes` ✅
- Idempotency implemented with stable snapshot ID generation in `src/db/snapshots.py` ✅
- `payload` enrichment requested (date range/columns context) is not explicitly populated from command flow ❌

**Verdict**: ❌ NON-COMPLIANT
**Reasoning**: Core checksum metadata integration exists, but payload enrichment scope described in task text is only partially realized.

### Task 5: Tests for Story 2-1
**Spec**:
- `tests/test_pipeline_snapshot.py` with ≥7 tests

**Actual**:
- `tests/test_pipeline_snapshot.py` exists with 7 tests ✅

**Verdict**: ✅ COMPLIANT

### Task 6: Tests for Story 2-2
**Spec**:
- `tests/test_snapshot_metadata.py` with ≥6 tests
- Must NOT test CLI flow (focus on metadata correctness)

**Actual**:
- File exists with 6 tests ✅
- Multiple tests invoke CLI command (`runner.invoke(... pipeline snapshot ...)`) ❌

**Verdict**: ❌ NON-COMPLIANT
**Reasoning**: Required test count met, but explicit task guardrail (“Do NOT test CLI flow here”) was violated.

### Task 7: `snapshots export` CLI
**Spec**:
- Create `src/snapshot_cli.py`, mount in `src/main.py`, implement CSV/JSON export
- Use `CliFeedback` for status and avoid direct `print()` status output

**Actual**:
- `src/snapshot_cli.py` created and mounted in `src/main.py` ✅
- CSV/JSON export behavior implemented ✅
- New code includes direct `typer.echo()` in `_SnapshotExportFeedback.start()` (`src/snapshot_cli.py:33`) ❌

**Verdict**: ❌ NON-COMPLIANT
**Reasoning**: Deliverables are implemented, but explicit output-pattern guardrail is violated in new CLI code.

### Task 8: CI validation script
**Spec**:
- Create `scripts/ci_validate_checksums.py`
- Validate checksums, handle missing file/no snapshots, exit 0/1

**Actual**:
- Script exists and implements DB scan + checksum comparison + summary + exit 0/1 ✅

**Verdict**: ✅ COMPLIANT

### Task 9: Tests for Story 2-3
**Spec**:
- `tests/test_snapshots_export.py` with ≥7 tests

**Actual**:
- File exists with 7 tests ✅

**Verdict**: ✅ COMPLIANT

### Task 10: Tests for Story 2-4
**Spec**:
- `tests/test_ci_validate_checksums.py` with ≥5 tests

**Actual**:
- File exists with >5 tests ✅

**Verdict**: ✅ COMPLIANT

### Task 11: Retention + `snapshots purge`
**Spec**:
- Create `src/retention.py` with 4 functions
- Add `purge` command to `src/snapshot_cli.py` with dry-run/confirm/archive behavior

**Actual**:
- `src/retention.py` exists with `get_retention_days`, `find_purge_candidates`, `archive_snapshots`, `delete_snapshots` ✅
- `snapshots purge` command implemented with required flags and mode checks ✅

**Verdict**: ✅ COMPLIANT

### Task 12: Tests for Story 2-5
**Spec**:
- `tests/test_retention_purge.py` with ≥10 tests

**Actual**:
- File exists with ≥10 tests ✅

**Verdict**: ✅ COMPLIANT

### Task 13: `pipeline restore-verify`
**Spec**:
- Implement command in `src/pipeline.py`
- JSON report, 4 checks, exit codes 0/1/2

**Actual**:
- `restore-verify` implemented in `src/pipeline.py` ✅
- Produces JSON report and 4 checks (`row_count`, `columns_present`, `checksum_match`, `sample_row_check`) ✅
- Exit code mapping 0/1/2 implemented ✅

**Verdict**: ✅ COMPLIANT

### Task 14: Tests for Story 2-6
**Spec**:
- `tests/test_restore_verify.py` with ≥7 tests

**Actual**:
- File exists with ≥7 tests ✅

**Verdict**: ✅ COMPLIANT

## Story Acceptance Criteria [6/6]

### Story 2-1
- Generate CSV from DB ✅
- Save snapshot under snapshots area ✅
- Return/emit snapshot path and metadata via CLI feedback and DB registration ✅

**Verdict**: ✅ MET

### Story 2-2
- Calculate SHA256 and register metadata ✅
- Persist checksum-linked metadata in snapshots table ✅

**Verdict**: ✅ MET

### Story 2-3
- Export CLI command for csv/json ✅
- stdout output supported when no `--output` ✅

**Verdict**: ✅ MET

### Story 2-4
- CI checksum validation script with pass/fail exit behavior ✅

**Verdict**: ✅ MET

### Story 2-5
- Retention with age-based candidate selection ✅
- `purge` command with dry-run/confirm/archive paths ✅

**Verdict**: ✅ MET

### Story 2-6
- `restore-verify` CLI with JSON report ✅
- 4 integrity checks + 3 exit codes ✅

**Verdict**: ✅ MET

## Must NOT Have Verification [1 issue]
- `src/cli/` directory: ✅ NOT FOUND
- SQLAlchemy imports (`from sqlalchemy` / `import sqlalchemy`): ✅ NOT FOUND
- REST framework imports (`flask` / `fastapi` / `django`): ✅ NOT FOUND
- Direct `print()` in new CLI code: ✅ NOT FOUND
- Direct `typer.echo()` in new CLI code: ❌ FOUND — `src/snapshot_cli.py:33`
- Inline `CREATE TABLE IF NOT EXISTS` in `src/db/snapshots.py`: ✅ NOT FOUND

## Unaccounted Changes [6 files]
Files modified/created that are not in Task 1–14 deliverable specs:

1. `.sisyphus/plans/epic-2-snapshots.md` — UNACCOUNTED/VIOLATION (plan declared read-only in work context)
2. `.sisyphus/boulder.json` — UNACCOUNTED
3. `pyproject.toml` — UNACCOUNTED (dependency drift outside task specs)
4. `poetry.lock` — UNACCOUNTED (lockfile drift outside task specs)
5. `tests/test_ingest_cache_incremental.py` — UNACCOUNTED (regression fix not in Epic 2 task deliverables)
6. `dados/data.db` — UNACCOUNTED runtime artifact drift

## Summary
- Tasks: **11/14 compliant**
- Must NOT Have: **1 issue**
- Unaccounted: **6 files**

Tasks [11/14 compliant] | Must NOT Have [1 issue] | Unaccounted [6 files] | VERDICT: REJECT

## VERDICT
**REJECT**

**Reasoning**:
Scope fidelity is not 1:1. There are explicit guardrail violations (direct `typer.echo()` in new CLI code), task-level compliance breaches (Task 6 and Task 7 guardrails; Task 4 partial metadata scope), and multiple unaccounted file changes including modification of the read-only plan file.
